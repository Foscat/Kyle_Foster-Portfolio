import { describe, it } from "vitest";

/**
 * Deprecated duplicate test entry.
 *
 * Canonical SectionRenderer coverage lives in:
 * `src/components/SectionRenderer/SectionRenderer.test.jsx`
 */

describe.skip("SectionRenderer duplicate suite", () => {
  it("is intentionally replaced by the canonical component test file", () => {});
});
